{
  "name": "mcp-unsandboxed-git-cli",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile ./dist/mcp-unsandboxed-git-cli"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
